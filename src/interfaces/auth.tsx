export type GoogleLoginData = {
    token: string;
    name: string;
    email: string;
    imageUrl: string;
};

export type FacebookLoginData = {
    token: string;
    name: string;
    email: string;
    imageUrl: string;
};
